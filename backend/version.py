"""
Application versioning utilities.

The `__version__` string is the single source of truth for the API version.
Update it via `scripts/bump_version.py` rather than editing manually.
"""

__version__ = "0.3.2"

